---
- name: Inicia Grafana, Loki, Promtail e Prometheus
  community.docker.docker_compose_v2:
    project_src: "{{ app_dir }}"
    state: present
    services:
      - grafana
      - loki
      - promtail
      - prometheus
    recreate: auto
