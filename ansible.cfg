[defaults]
# Temporary config removed: no suppression of deprecation warnings
# Keep this file intentionally minimal in case CI/runner needs project-local settings
# (Currently empty to avoid hiding legitimate deprecation warnings)
